{
  "name": "comp.cs.200",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "test": "mocha \"test/**/*.test.js\"",
    "coverage": "nyc --check-coverage mocha \"test/**/*.test.js\"",
    "coverage-report": "nyc report --reporter=text-lcov > coverage.lcov && coveralls < coverage.lcov"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.1.1",
    "mocha": "^10.8.2",
    "nyc": "^17.1.0"
  },
  "dependencies": {
    "@types/node": "^22.9.0",
    "chai": "^5.1.2",
    "node": "^22.11.0"
  }
}
